import os.path as osp

FLANN_DIR = osp.join(osp.dirname(__file__))
FLANN_SRC_DIR = osp.join(FLANN_DIR, 'src')
FLANN_BUILD_DIR = osp.join(FLANN_DIR, 'build')
FLANN_LIB_DIR = osp.join(FLANN_DIR, 'lib')
